co to gunicorn w skrocie, to jest serwer ktory zainstalowany na maszynie (w obrazie) pozwala laczyc z sie z maszyna?



do czego sluzy __init__.py



do czego jest biblioteka os



dlaczeego w wsgi.py  z app moge zaimportowac create app, kiedy ona jest w __init__.py

"from app import create_app"



dlaczego wsgi nazwa? skrot do czego?



dlaczego jest opis "# Inicjalizacja rozszerzeń, ale bez przypisywania do konkretnej aplikacji", chodzi mi o sformuloawnie do konrentej aplikacji, zwykle jest wiecej aplikacji czy jak, podaj przyklad, 2. init extenstions (przypisanie do aplikacji), dlaczego przypisujemy to do apki? dlaczego kazdy obiekt ma metode init_app?



co to CORS, czy to sluzy do komunikacji miedzy bcknd a fronent? podaj przyklad



dlaczego rejestrujemy Blueprints? co to robi? przyklad



co to jest funckaj def __repr__(self), po co jest?

do czego sluzy kernel? dlaczego mimo ze venv jest odpalone pokazuje mi ze bilbioteki ktore sa wgrywane do __init.py nie istnieja?

dlaczego w routes.py w ten sposób twozymy bluepint dla api, dlaczego wchodzi do funkcji 'api', __name__?
# Utworzenie Blueprint dla API
api = Blueprint('api', __name__)

jak działa generowanie tokenu, po co on jest , co dzięki niemu mamy, czemu niie wpuszamyu po prostu haslo login
    # verify password and user existence
    if user and bcrypt.check_password_hash(user.password_hash, password):
        # generate token JWT (dane, które będą w tokenie to id użytkownika)
        access_token = create_access_token(identity=user.id)
        return jsonify(access_token=access_token), 200
    else:
        return jsonify({"msg": "Bad username or password"}), 401

jak to sie dzieje ze we funkkach POST dane z requestu juz sa w request.get_json(), bezposrednio z bilioteki request hmm?

